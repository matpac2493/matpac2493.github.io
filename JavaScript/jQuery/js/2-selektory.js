$( function() {
    // kod do wykonania
// pobieranie paragrafów
    let paragraphs = $("p")
    console.log(paragraphs);

// pobieranie po id

    let idElement = $( "#paragraf" );
    console.log(idElement);

// pobieranie po klasie

let elementClass = $("paragrafnext");
console.log(elementClass)















    } );